# 2格武器OA修复 - 验证检查清单

## 代码层面验证 ✓ 完成

### 1. HexCoord.ring() 函数
- [x] 函数已添加到 scripts/core/HexCoord.gd (行69-102)
- [x] 使用立方体坐标正确计算六边形环
- [x] 边界条件处理 (distance=0, distance<0, distance=1)
- [x] 返回类型正确 (Array[Vector2i])

### 2. HexGrid.get_zoc_controllers()
- [x] 改动在 scripts/core/HexGrid.gd (行429-459)
- [x] 从 neighbors() 循环改为遍历所有单位
- [x] 使用 HexCoord.distance() 计算距离
- [x] 读取 weapon.range_max 属性
- [x] 保留默认 ZoC=1 的向后兼容
- [x] 保留远程武器不产生 ZoC 的逻辑

### 3. HexGrid.get_zoc_cells_of()
- [x] 改动在 scripts/core/HexGrid.gd (行464-501)
- [x] 使用 HexCoord.ring() 获取多环
- [x] 正确处理 zoc_distance=1 的快速路径
- [x] 去重逻辑正确（seen 字典）
- [x] 所有范围内的格子都被包含

### 4. 调用链自动修复
- [x] analyze_path_oa() 会自动调用修复后的 get_zoc_controllers()
- [x] Unit._animate_path() 会自动调用修复后的 analyze_path_oa()
- [x] BattleAI.decide() 会自动调用修复后的 get_zoc_controllers()
- [x] BattleScene UI 会自动调用修复后的 get_zoc_cells_of()

---

## 游戏内功能验证 (待测)

### 基础功能测试
- [ ] **游戏启动**: 没有编译错误
- [ ] **创建战场**: 地图加载正常
- [ ] **放置单位**: 包括持 1 格和 2 格武器的单位

### 1 格武器测试（回归）
- [ ] **短剑单位**
  - 相邻格(距离1): 移动通过时触发借机 ✓
  - 距离2格以外: 不触发借机 ✓
- [ ] **匕首单位**
  - 相邻格(距离1): 移动通过时触发借机 ✓
- [ ] **长棍单位**（改为测试 range [1,2] 之前）
  - ...（当前应该还没有长棍，或 range 仍为 [1,1]）

### 2 格武器测试（核心）
- [ ] **长矛单位**（range [1,2]）
  - **距离1格**: 移动通过时触发借机 ✓
  - **距离2格**: 移动通过时触发借机 ✓ ← **这是关键修复**
  - **距离3格**: 不触发借机 ✓
- [ ] **长棍单位**（range [1,2]）
  - 同上

### UI 高亮验证
- [ ] **威胁地图高亮**
  - 选中持短剑单位: 高亮只显示相邻 1 格 ✓
  - 选中持长矛单位: 高亮显示 2 格范围（包括相邻+距离2的环）✓
- [ ] **路径预览高亮**
  - 计划穿过距离2的长矛敌人: 路径上应显示"借机触发"标记 ✓

### 战斗行为验证
- [ ] **借机攻击执行**
  - 触发时血量显示、伤害计算正确
  - "命中即停"规则（被击中则路径中断）
- [ ] **借机次数**
  - 穿过一个 2 格长矛: 1 次借机 ✓
  - 穿过多个长矛: 多次借机 ✓
  - 叠加计算正确（先受长矛1的借机，再受长矛2的借机）

### AI 决策验证
- [ ] **威胁评估**
  - AI 倾向避开长矛的 2 格范围（而非只避开 1 格）
  - 同样距离下，长矛敌人的威胁值应高于短剑敌人
- [ ] **路径选择**
  - AI 会绕路避免通过长矛 2 格范围（如果有其他路径）
  - 被逼进入 ZoC 时表现更谨慎

---

## 性能验证

- [ ] **帧率**: 不低于修复前（应无明显差异）
  - 单位数 ≤ 20 时不应有感知
- [ ] **路径计算时间**: 应在毫秒级
- [ ] **UI 响应**: 威胁地图高亮秒出，无卡顿

---

## 回归测试

### 友方掩护
- [ ] 被同阵营单位"掩护"的敌人不产生借机 (逻辑不变)

### 远程武器
- [ ] 弓手、弩手不产生 ZoC (weapon_type != "melee" 检查不变)
- [ ] 远程敌人在任何距离都无法触发借机

### 地形障碍
- [ ] 无法跨越障碍的路径不被规划
- [ ] 借机计算不受地形影响（仅按直线距离）

---

## 已知限制 (预期行为)

1. **weapon.range_max 加载**
   - 预期: WeaponArmorDB.gd 正确设置 range_max (已验证 ✓)
   - 风险: 低

2. **HexCoord.ring() 算法**
   - 预期: 立方体坐标绕行计算正确 (需游戏内验证)
   - 风险: 中（复杂算法）
   - 测试方法: `HexCoord.ring(Vector2i(0,0), 2).size() == 12` (应为 2 格环的 12 个格子)

3. **性能 O(units)**
   - 预期: 影响可接受 (单位数 ≤ 20)
   - 风险: 低

---

## 问题排查指南

### 症状: 2 格武器仍无法触发借机

**检查清单:**
1. 确认武器数据: data/weapons.json 中长矛是否有 `"range": [1, 2]`
2. 确认武器加载: 启动时是否正确加载了 range_max=2
3. 验证 HexCoord.ring():
   ```gdscript
   var r = HexCoord.ring(Vector2i(0, 0), 2)
   print("Distance 2 ring size: ", r.size())  # 应为 12
   ```
4. 验证 get_zoc_controllers():
   ```gdscript
   # 在 Unit._animate_path 中添加调试打印
   var ctrls = hex_grid.get_zoc_controllers(target_cell, faction)
   print("ZoC controllers at %s: %d" % [target_cell, ctrls.size()])
   ```

### 症状: UI 威胁高亮不完整

**检查清单:**
1. 确认 get_zoc_cells_of() 被调用
2. 验证返回的格子数: 距离1格单位应返回 6 个; 距离2格单位应返回 18 个 (6+12)
3. 检查是否有格子过滤逻辑遮挡了高亮

### 症状: 性能下降

**检查清单:**
1. 验证单位数是否 > 50
2. 增加缓存: 若 get_zoc_controllers() 被频繁调用，可考虑 per-turn 缓存
3. 使用 Profiler 定位瓶颈

---

## 测试通过标准

✓ **所有基础功能测试通过**
✓ **所有 UI 高亮验证通过**
✓ **战斗行为正确**
✓ **AI 决策改进**
✓ **无回归问题**
✓ **性能可接受**

---

## 最后签核

| 项目 | 状态 | 负责人 | 完成时间 |
|------|------|--------|---------|
| 代码修复 | ✓ 完成 | Claude | 2026-06-09 |
| 代码审查 | ⏳ 待 | — | — |
| 游戏内测试 | ⏳ 待 | — | — |
| 性能验证 | ⏳ 待 | — | — |
| 发布前检查 | ⏳ 待 | — | — |

---

